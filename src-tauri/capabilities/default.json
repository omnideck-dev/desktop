{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the dashboard window — enumerated command allowlist (dashboard-bridge) instead of core:default's broad Tauri baseline. core:event:default is needed for the add/update/remove progress listeners (NewDeckForm.tsx, RemoveDeckDialog.tsx); opener:default for external links (ExternalPage.tsx).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:event:default",
    "opener:default",
    "dashboard-bridge"
  ]
}